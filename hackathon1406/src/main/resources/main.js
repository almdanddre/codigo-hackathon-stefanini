function validateStudents(e) {
    const input = document.querySelectorAll('.students input');
    const form = document.querySelector('.studentsForm');
    const btn = document.querySelector('.students button');
    let check;

    

    
}